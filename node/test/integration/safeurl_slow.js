describe('safeurl integration tests', function() {
    it('parses a safe url correctly', function() {
        // call it with a safe url and check the response
    })

    it('parses an unsafe url correctly', function() {
        // inject a bad url into redis, 
        // call it with the bad url and check the response
    })

})